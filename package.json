{
  "name": "hardhat-project",
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat ignition deploy --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.1"
  }
}
